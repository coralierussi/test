## EXO

``` bash
git init
echo "# test" >> README.md
git add .
git commit -m "create file"
git branch -M main
git remote add origin https://github.com/coralierussi/test.git
git push -u origin main
git push
git checkout -b exo
git status
git checkout main

git checkout exo
git add .
git commit -m
git rebase main

```
